<div class="container">
    <h2><%= tableName %> 자료 목록</h2>
    <% if (rows.length === 0) { %>
        <p>테이블에 자료가 없으니 자료를 추가해주십시오.</p>
    <% } else { %>
        <table class="table table-bordered" style="text-align: center; vertical-align: middle; border-color: #007bff;">
            <thead>
                <tr>
                    <% for (var i = 0; i < comments.length; i++) { %>
                        <th style="border: 2px solid #007bff;"><%= comments[i].COLUMN_COMMENT %></th>
                    <% } %>
                    <th colspan="3" style="border: 2px solid #007bff;"></th>
                </tr>
            </thead>
            <tbody>
                <% for (var i = 0; i < rows.length; i++) { %>
                    <tr style="border: 2px solid #007bff;">
                        <% for (var j = 0; j < comments.length; j++) { %>
                            <td style="border: 2px solid #007bff;"><%= rows[i][comments[j].COLUMN_NAME] %></td>
                        <% } %>
                        <td style="border: 2px solid #007bff;"><a href="/<%= tableName %>/create/<%= rows[i][Object.keys(rows[i])[0]] %>">생성</a></td>
                        <td style="border: 2px solid #007bff;"><a href="/<%= tableName %>/update/<%= rows[i][Object.keys(rows[i])[0]] %>">수정</a></td>
                        <td style="border: 2px solid #007bff;"><a href="/<%= tableName %>/delete/<%= rows[i][Object.keys(rows[i])[0]] %>">삭제</a></td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    <% } %>
</div>
